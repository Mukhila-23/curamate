<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Curamate">

        <!-- Activities -->
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AvatarSetupActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".MedicineActivity" />
        <activity android:name=".AddMedicineActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".SleepActivity" />
        <activity android:name=".MealActivity" />
        <activity android:name=".CustomiseMealActivity" />
        <activity android:name=".ExerciseActivity" android:exported="true" />
        <activity android:name=".CustomiseExerciseActivity" android:exported="false" />
        <receiver android:name=".WakeAlarmReceiver"
            android:exported="true"/>
        <activity android:name=".WakeAlarmActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"/>


        <!-- Receivers -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
        <receiver
            android:name=".ExerciseReminderReceiver"
            android:exported="true"
            android:enabled="true"/>

        <receiver
            android:name=".MealReminderReceiver"
            android:enabled="true"
            android:exported="true"/>

        <receiver
            android:name=".MedicineReminderReceiver"
            android:enabled="true"
            android:exported="true"/>

        <receiver
            android:name=".SleepNotificationReceiver"
            android:enabled="true"
            android:exported="true"/>
        <receiver
            android:name=".SleepReminderReceiver"
            android:enabled="true"
            android:exported="true"/>


    </application>

</manifest>